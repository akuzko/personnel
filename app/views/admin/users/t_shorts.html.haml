.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", admin_users_path
      %li.active= link_to "#{t("web-app-theme.t-shorts", :default => "T-Shorts")}", t_shorts_admin_users_path
      %li= link_to "#{t("web-app-theme.new", :default => "New")}", new_admin_user_path
  .content
    %h2.title
      = t("web-app-theme.all", :default => "All")
      T-Shorts
    .inner
      %table.table
        - department = ''
        - @users.each do |user|
          - if department != user.name
            - department = user.name
            %tr
              %td{:colspan => 6}
                %h3= department
            %tr
              %th
                = t("activerecord.attributes.profile.t_short_size", :default => t("activerecord.labels.size", :default => "Size"))
              %th
                = t("activerecord.attributes.admin_event.count", :default => t("activerecord.labels.count", :default => "Count"))
          %tr{:class => cycle("odd", "even")}
            %td
              = user.t_short_size
            %td
              = user.total
- content_for :sidebar, render(:partial => 'sidebar')
